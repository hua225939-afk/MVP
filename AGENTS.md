# Vibe Coding 长期开发规则

## 范围与架构

- 课程内容必须与页面代码分离。
- 不把 13 节课逐页写死。
- 所有课次由 JSON 和 `LessonRenderer` 渲染。
- 互动组件必须可复用，不为单个课次复制专用组件；新增互动类型时同步更新 Zod Schema、组件测试和 `LessonRenderer` 分发。
- “造物星球·创造台”是课程系统的持续项目编辑层，不是与课程分离的第二套系统；课程页和创造台必须通过统一 `ProjectRepository` 读写同一份 `ProjectDocument`。
- 第 01 课创建课程主项目，第 02—13 课继续修改同一个 `projectId`；不得把 13 课实现成 13 份互不相关的作品或 localStorage 记录。
- 创造工具统一注册，明确课次、解锁条件、输入输出、测试规则、项目字段权限和 React 组件；不得在课程页或 `LessonRenderer` 中为单课硬编码工具行为。
- 五个角色端共用同一套模拟数据，页面不得各自维护冲突的统计和实体副本。
- 第一阶段不加入真实登录、数据库、支付和真实 AI 接口。

## 修改纪律

- 使用现有 MVP 继续扩展，不重新建立网站。
- 不删除现有可用功能。重构、路由迁移或存储迁移前，先说明影响、兼容方案和验收方式。
- 修改只覆盖当前任务需要的文件，不顺带重构无关代码。
- 未确认的产品行为标记为 `TODO`，不得自行扩大范围。
- 五端框架与第 01、06 课样板已完成；后续按“创造台底层架构 → 第 01—05 课及工具 → 第 06—09 课及工具 → 第 10—13 课及工具 → 五端动态联动 → 全站测试与部署”推进。
- 课程、项目、模拟业务数据和用户学习状态分层：课程放 `content/`，持续作品使用 `ProjectDocument`，统一模拟数据放共享数据层，项目与学习进度分别通过稳定存储接口读写。
- 教师课程预览必须只读，不得写入学生学习进度或作品。
- 项目字段变更必须生成可校验、可审计的 patch；工具只能修改注册表声明的字段，上游修改后相关测试结果必须失效并要求重测。

## 验证

- 每次修改后运行项目已有的 lint、类型检查和 build 命令。
- 当前命令为 `npm run lint`、`npx tsc --noEmit` 和 `npm run build`；若后续增加 `npm run typecheck`，优先使用项目脚本。
- 修改课程 JSON 或 Schema 时，必须同时运行课程内容验证和相关测试。
- 修改 `ProjectDocument`、工具注册表或项目存储时，必须运行 Schema、字段权限、同步、迁移和教师只读契约测试。
- 页面修改至少检查电脑与平板布局。
