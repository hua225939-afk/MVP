import { CommonRolePage } from "@/components/platform/CommonRolePages";

export const metadata = { title: "设置" };

export default function SettingsPage() {
  return <CommonRolePage kind="settings" />;
}
